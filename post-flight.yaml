- name: Post-flight for Linux
  hosts: localhost
  tasks:
    - name: Linux Block
      when: ansible_os_family in ["RedHat", "Debian"]
      block:
        - name: pwd 
          ansible.builtin.shell: pwd
        - name: who
          ansible.builtin.shell: who
        - name: check patchfile
          ansible.builtin.shell: cd ~ && ls
          register: pf
        - name: Distribution major version
          debug: var=pf.stdout_lines
